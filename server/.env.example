PORT=4000
TAX_RATE=0.08

# Required in production — the server refuses to start without it when
# NODE_ENV=production, and requires at least 32 characters.
# Generate one with:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=
