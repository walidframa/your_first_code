# Fail fast on an unsupported Node instead of part-way through a native build.
# better-sqlite3 is a compiled dependency and only ships prebuilt binaries for
# released Node ABIs; on a newer Node it falls back to a source build that
# usually fails with a confusing node-gyp error.
engine-strict=true
