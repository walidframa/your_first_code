# Fail fast with a readable EBADENGINE error on an unsupported Node, rather than
# part-way through an install chain. The database layer uses node:sqlite, which
# is only available unflagged from Node 24.
engine-strict=true
