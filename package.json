{
  "name": "front-desk-pos",
  "version": "1.0.0",
  "private": true,
  "description": "A point-of-sale web app with checkout, inventory, orders and role-based staff accounts",
  "scripts": {
    "setup": "npm install && npm --prefix server install && npm --prefix client install && npm run seed",
    "seed": "npm --prefix server run seed",
    "dev": "concurrently -n server,client -c green,cyan \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "test": "npm --prefix server test",
    "e2e": "node e2e/run.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "playwright": "^1.56.0"
  }
}
